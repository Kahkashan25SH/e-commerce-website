// app/cart.tsx or pages/cart.tsx
import CartPage from '../app/cart/page'; // Adjust the import as per your structure

const Cart = () => {
  return <div>
    <CartPage />
    </div>;
};

export default Cart;
